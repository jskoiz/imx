# IMX v0.5.0 Release-Ready Checklist

Status: pre-tag candidate. Do not create or push `v0.5.0` until the user
explicitly approves that exact release action in the current turn.

## Release Surface

- Binary: `imx`.
- Formats: FARBFELD, QOI, PBM, PGM, PPM.
- Commands: `imx identify <input>` and `imx <input> <output>`.
- Compatibility delta: deterministic same-format rewrites for the five
  supported formats when input and output paths differ.
- Planned hosted release targets: `x86_64-unknown-linux-gnu` and
  `aarch64-unknown-linux-gnu`.

## Required Pre-Tag Evidence

- `bash scripts/check-no-hosted-apple-actions.sh`
- `bash scripts/smoke-homebrew-formula-generator.sh`
- `bash -n scripts/*.sh`
- `git diff --check`
- `cargo fmt --all -- --check`
- `cargo fmt --manifest-path fuzz/Cargo.toml --all -- --check`
- `cargo check --workspace`
- `cargo check --manifest-path fuzz/Cargo.toml`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `bash scripts/smoke-same-format-determinism.sh`
- `IMAGEMAGICK_MAGICK=target/local-tools/magick-oracle IMX_REQUIRE_ORACLE=1 IMX_FUZZ_MAX_TOTAL_TIME=2 IMX_BENCH_ITERATIONS=3 bash scripts/ci.sh`
- `IMAGEMAGICK_MAGICK=target/local-tools/magick-oracle IMX_BENCH_ITERATIONS=3 bash scripts/bench-release.sh`
- `IMAGEMAGICK_MAGICK=target/local-tools/magick-oracle IMX_BENCH_BASE_REF=v0.4.0 IMX_BENCH_ITERATIONS=3 bash scripts/bench-regression.sh`
- `IMX_INSTALL_REPO_URL=/Users/jk/Desktop/imx IMX_INSTALL_REVISION=HEAD bash scripts/verify-install.sh`
- `bash scripts/package-release.sh`
- `IMX_RELEASE_DIR=target/release-artifacts IMX_RELEASE_TARGET=$(rustc -vV | sed -n 's/^host: //p') bash scripts/verify-release-archive.sh`

## Approval Gate

If every pre-tag gate passes, report the release as ready and wait. Publishing
requires explicit approval to create and push the `v0.5.0` tag.

## Post-Publish Evidence

After approval and tag publication, verify the published Linux x86_64 and Linux
arm64 archives from GitHub Release assets, then update `jskoiz/homebrew-imx`
from the published `SHA256SUMS`.
